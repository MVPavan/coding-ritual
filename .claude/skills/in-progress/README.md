# in-progress

Draft skills parked here are NOT loaded by the skill loader or the catalog (discovery is one level: `skills/*/SKILL.md`). Promote a skill by moving its folder up one level and regenerating the catalog (`python3 .claude/scripts/skill-catalog.py --write`).

- `use-codex/` — parked; the Codex-side workflow kept for possible reactivation (see `.claude/project/tools.md`).

`agent-matrix/` was promoted out of here on 2026-09-21 after its Claude review.
