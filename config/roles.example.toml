# Human role bindings. Copy to the rendered wrapper root once, then edit there.
# Model IDs and efforts must appear in the qualified local catalog.
[roles.implementer]
model = "claude-opus-5-5"
effort = "medium"
# apply = "now"  # default: "next-task"; now applies at the next activation

[roles.critic]
model = "gpt-6-sol"
effort = "high"

[roles.scribe]
model = "claude-fable-5-1"
effort = "low"

[roles.test-author]
model = "claude-opus-5-5"
effort = "medium"

[roles.test-critic]
model = "gpt-6-sol"
effort = "high"

[roles.impl-critic]
model = "gpt-6-sol"
effort = "high"
