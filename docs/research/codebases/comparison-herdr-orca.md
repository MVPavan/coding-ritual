# Herdr / Orca — agent control plane decision

> Repositories: https://github.com/herdrdev/herdr at `9c96f7ddb3be2cc575a159d4d1f1d49fb10d7006`; https://github.com/stablyai/orca at `5e3effc32f9fa9982749ea17333c885bff1984f9` · 2026-09-24. Method: GPT-6 Luna high, static read-only, two passes; no live probes. Source references use pinned `herdr:` / `orca:` paths. `[read]` means observed source; `[inferred]` marks the ruling or implication.

## Question and boundary

Can Herdr or Orca replace the workflow interpreter's agent control plane—inspector, crew profiles, and foreman dispatch—while everything else stays unchanged?

The foreman mints a keyed graph-node activation and owns the deterministic SQLite ledger. The inspector runs one headless vendor CLI process per turn (`claude -p --output-format stream-json` or `codex exec --json`), with pinned model/effort, isolation flags, and a worktree. Claude's profile mints a UUID before launch and passes `--session-id`; Codex instead captures `thread.started` from its stream. Both resume the vendor session on a later activation. The inspector proves and pins input/output git trees, grades the structured exit log into success/fail/doubt plus final message and diff, and routes on that result. Steering is a new continuation turn, not terminal typing; delivery and recovery are keyed and crash-safe. `workflow_interpreter/profiles/claude.py:198` `workflow_interpreter/profiles/codex.py:197` `workflow_interpreter/inspector/launch_record.py:24`

## Verdict

| Candidate | Ruling | Why |
|---|---|---|
| Herdr | **REJECT as control plane; DEFER human live-view/attach** | [read] It owns PTYs and terminal observations; Codex can remain unknown, prompt waits do not delimit a turn, `pane.exited` lacks exit code/result, and the event ring is ephemeral. [inferred] Native attach would require PTY ownership and conflict with our pipe-based CLIs; a pane tailing our existing event log/transcripts could offer cheaper human visibility without owning the agent, but would not be an attached terminal. `herdr:src/pane.rs:2271` `herdr:src/detect/manifests/codex.toml:6` `herdr:src/api/wait.rs:211` `herdr:src/api/schema/events.rs:526` `herdr:src/api/event_hub.rs:1` |
| Orca | **REJECT as platform; BORROW mechanisms** | [read] Structured Claude SDK/Codex app-server sessions, persistent journals, resume, dispatch receipts, and mailboxes make it the closer fit. But `orca serve` launches Electron; structured sessions keep provider connections across turns, task success is `worker_done` self-report, and no per-turn git-tree pin appears in inspected paths. Orchestration is documented as Experimental behind a Settings toggle. [inferred] Its task/dispatch SQLite duplicates our ledger, no published API stability promise was found, and runtime singleton dependencies bar drop-in library use. `orca:src/cli/runtime/launch.ts:80` `orca:src/main/codex/codex-app-server-connection.ts:32` `orca:src/main/codex/codex-structured-session-state.ts:91` `orca:src/main/runtime/orchestration/lifecycle-reconciliation.ts:277` `orca:src/main/runtime/orchestration/db/worker-dispatch/worker-dispatch-start.ts:46` `orca:docs/site/content/docs/cli/orchestration.mdx:12` `orca:src/main/runtime/rpc/methods/structured-agent-session-create.ts:25` |

Orca's structured provider path is a clear advantage over Herdr's screen state: it receives Claude's result or Codex `turn/completed` and retains journal history. Our own Claude `stream-json` and Codex `exec --json` profiles already parse structured frames, and a frozen `codex-appserver` crew exists. Orca therefore adds no missing structured-turn foundation for us; adopting its runtime would still change our launch contract and leave artifact grading with us. `orca:src/main/claude/claude-turn-lifecycle-item.ts:23` `orca:src/main/codex/codex-app-server-notification-schema.ts:18` `workflow_interpreter/profiles/claude.py:302` `workflow_interpreter/profiles/codex.py:352` `workflow_interpreter/profiles/codex_appserver.py:43` [read]/[inferred]

## Requirement matrix

`Supported` means the specific requirement is met by an observed path; `Partial` means useful machinery exists but changes our contract or needs our own enforcement; `Missing` means no matching mechanism was found in inspected paths. Negative findings are bounded by this static reading.

| Our requirement | Herdr | Orca |
|---|---|---|
| Foreman mints one graph-node activation; deterministic SQLite ledger | **Missing** [inferred]: pane snapshots, no activation ledger. `herdr:src/persist/snapshot.rs:99` | **Partial** [read]: Task/Dispatch DB exists, but would be a second ledger. `orca:src/main/runtime/orchestration/db/worker-dispatch/worker-dispatch-start.ts:46` |
| Headless CLI, one process per turn, exact model/effort/isolation/worktree | **Partial** [read]: direct argv/cwd/env, but PTY; model/effort caller-supplied. `herdr:src/pane.rs:2271` | **Partial** [read]: structured no-TUI launch with options/worktree, but a provider connection spanning turns. `orca:src/main/codex/codex-app-server-connection.ts:32` `orca:src/main/codex/codex-structured-turn-start.ts:105` |
| Assign or capture vendor session ID and resume later | **Partial** [read]: hooks store native reference; restore types resume into new pane. `herdr:src/integration/targets.rs:124` `herdr:src/app/agent_resume.rs:226` | **Supported** [read]: Claude/Codex identities persist and later acquisition resumes. `orca:src/main/claude/claude-structured-init-proof.ts:22` `orca:src/main/native-chat/agent-session-wire/structured-agent-session-restart-restore.ts:47` |
| Prove/pin input and output git trees per turn | **Missing** [inferred]: worktree operations, no per-turn pin in inspected paths. `herdr:src/worktree.rs:239` | **Missing** [inferred]: worker worktree creation, no per-turn git-object/ref pin in inspected paths. `orca:src/main/runtime/rpc/methods/orchestration/worker/worker-worktree-creation.ts:45` |
| Parse exit log to success/fail/doubt, final message, diff; route on result | **Missing** [read]: terminal state/text and no caller-visible exit result. `herdr:src/api/schema/events.rs:526` | **Partial** [read]/[inferred]: journaled provider outcome/messages/diffs, but no independent activation/artifact grade. `orca:src/main/codex/codex-structured-item-translation.ts:92` `orca:src/main/runtime/orchestration/lifecycle-reconciliation.ts:277` |
| Steering is a new continuation turn, no PTY typing | **Missing** [read]: `agent.prompt` types into PTY. `herdr:src/app/api/agents.rs:111` | **Supported** [read]: structured `agentSession.send` submits provider turn. `orca:src/main/runtime/rpc/methods/structured-agent-session.ts:200` |
| Idempotent-by-key and crash-recoverable | **Missing** [inferred]: snapshots plus volatile event ring cannot settle an ambiguous prompt. `herdr:src/api/event_hub.rs:1` | **Partial** [read]: keyed durable dispatch and explicit unknowns; provider effects remain outside the transaction. `orca:src/main/runtime/orchestration/db/worker-dispatch/worker-dispatch-start.ts:48` |

## Worth borrowing, ranked

Effort is an engineering estimate for adapting a mechanism inside our existing inspector/ledger, not for installing either platform. Items 1–3 are conditional studies, not identified gaps in current activation safety.

1. **Explicit provider-submission states: `pending/accepted/rejected/unknown`, without automatic resend.** Orca reconciles uncertain sends from provider history. Our pre-exec `LaunchReceipt` already identifies a launch, including the `REATTACHED` window, and §5.6 recovery classifies liveness without a crash loop. The possible delta is finer provider-acknowledgement evidence *within* a launched CLI turn; defer until a real unresolved continuation case shows our exit-log recovery insufficient. **Effort: medium.** `orca:src/shared/agent-session-journal-types.ts:337` `orca:src/main/native-chat/agent-session-journal/journal-pending-submission-recovery.ts:4` `workflow_interpreter/inspector/launch_record.py:24` `workflow_interpreter/inspector/recover.py:1` [read]/[inferred]
2. **Mutation payload receipts keyed by `(callerFingerprint, requestId)`.** Orca returns `request_mismatch` for a changed request and `operation_unknown` for accepted replay. Our deterministic natural keys already make a re-tick find an existing activation, and mint facts derive from the pinned graph/trace. The narrow delta would be rejecting changed payloads under an *external caller-supplied request ID* if such a control API is added; do not duplicate our current mint identity. **Effort: medium if that API appears; otherwise defer.** `orca:src/main/runtime/orchestration/db/worker-dispatch/worker-dispatch-start.ts:48` `workflow_interpreter/bdio/keys.py:1` `workflow_interpreter/bdio/mint.py:116` [read]/[inferred]
3. **Per-session SQLite journal with WAL and `synchronous=FULL`.** Orca stores provider messages and turn state in a durable session journal. Our activation ledger and exit log already preserve turn evidence; the possible delta is a resumable, queryable cross-activation provider history only if a concrete recovery/query need emerges. Avoid a competing task authority. **Effort: medium to high; defer.** `orca:src/main/native-chat/agent-session-journal/journal-database.ts:23` `workflow_interpreter/inspector/recover.py:21` [read]/[inferred]
4. **Exact-process-incarnation mailbox pointer delivery.** Orca reserves a pointer for a specific PTY/process, settles accepted writes, and leaves unverifiable Enter ambiguous. For us: adapt the fencing and ambiguity states only if future inter-agent messaging needs a mailbox; our current continuation path does not type into PTYs. **Effort: high.** `orca:src/main/runtime/orchestration/mailbox-pointer-submit.ts:59` `orca:src/main/runtime/orchestration/mailbox-pointer-submit.ts:138` [read]/[inferred]
5. **Per-agent screen manifests as optional human visibility.** Herdr's Claude/Codex manifests show activity/blockers in an attached terminal, but unmatched Codex can stay unknown. For us: first try a pane tailing existing logs/transcripts for human visibility; a native attached terminal needs PTY ownership and must never route or grade work. **Effort: low for log tail, high for PTY integration; defer.** `herdr:src/detect/manifests/claude.toml:7` `herdr:src/detect/manifests/codex.toml:6` `herdr:src/pane.rs:2271` [read]/[inferred]

## Open points and limits

- [read] This was one explorer model, two static read-only passes, with no live provider or restart probes. The detailed evidence is in `herdr/luna-exploration.md` and `orca/luna-exploration.md`; negative findings mean only that the searched paths did not show the mechanism.
- [read] Orca's structured Codex path uses a persistent app-server connection. Our `codex-appserver` crew remains registered but frozen and outside the current model-catalog migration; revisiting Orca's mechanism would require a separate decision about that crew. `orca:src/main/codex/codex-app-server-connection.ts:32` `orca:src/main/codex/codex-structured-session-state.ts:91` `docs/workstreams/model-catalog/design.md:12`
- [inferred] A live fit probe would need to run exact pinned model/effort and isolation options; record stdout/stderr, exit code, provider session ID, final result/diff, and input/output git refs across a crash after submission but before settlement. For Herdr, test PTY versus pipe output and ambiguous prompt waits. For Orca, test Electron `serve` startup, restart/history reconciliation, duplicate request receipts, and whether a reported `worker_done` can disagree with independently graded artifacts. `herdr:src/api/wait.rs:211` `orca:src/main/native-chat/agent-session-journal/journal-restart-reconciliation.ts:79` `orca:src/main/runtime/orchestration/lifecycle-reconciliation.ts:277`
