# P4 verification — 2026-09-11

Fresh integration passes the repository gates: 1536 non-live tests, 143 process tests, 24 Beads tests and 66 acceptance tests. Families overlap. Ruff and formatting pass over 209 files; strict mypy passes over 108 source files.

The first process run found a test synchronization race: cancellation and exit were already recorded while the wrapper still held its band to finish late evidence. The corrected test waits for the actual lifecycle barrier and bounded wrapper completion before asserting the mint fence. Both parametrizations and all 11 child process tests passed, followed by the complete 143-test process gate. Runtime sources did not change; the source inventory differed only in this test. The earlier full unit and other five successful checks remain applicable; the corrected process suite supplies fresh coverage of that change.

Integration records an explicit immutable set of collected sources in the original Beads owner ledger, reserves one integration member, and uses the existing bridge for fresh review, pinned checks, authenticated ship approval, target CAS and closure. Tests cover code and Markdown contributions, final-reader receipts, an independent target edit, cancellation ordering, stale authority, crash recovery and same-pins retry. Collection alone never authorizes landing.

Evidence is retained in the ignored execution workspace: `p4-implementation-report.md`, `p4-final-*.log`, `p4-proc-corrected.log`, `p4-test-sync-report.md`, `p4-corrected-results.json` and `p4-complete-source-digests.json`. The final grouped Fable review of P4/P5 remains a workstream exit requirement. P5 owns public integration admission commands, trusted replacement, ordinary templates and the assembled real-runtime proof. No development commit, merge or push.
