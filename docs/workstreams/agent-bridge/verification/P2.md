# P2 verification — 2026-09-11

Bounded ordinary model decisions and byte-bounded handoffs passed their phase exit. Distinct doubt vocabulary remains P5; child/bridge replacement bindings remain P4/P5.

- Full non-live suite: 1457 passed, 28 deselected.
- Process: 123 passed; Beads: 22 passed; acceptance: 66 passed (overlapping families, not additive).
- Ruff and format clean (200 files); strict mypy clean (106 source files).
- Real host decision proof: one test passed in 187.91 seconds. Actual Codex decision consumed once, continued declared work, produced `{ "sum": 55 }` in the recorded output tree and left the human ship gate open and unsigned. Saved session metadata verifies `gpt-6-astra`.

Evidence is retained in the ignored execution workspace: `p2-final-results.json`, `p2-final-*.log`, `p2-live-host-final.log`, `p2-terra-handoff.md`, and `p2-complete-source-digests.json`. The disposable host fixture is identified by the live pytest command recorded in the session handoff; its `runtime-evidence.json` contains root, activation, output-tree and actual model-session identities.

Earlier failures are retained: worker sandbox initialization failed before model execution; the first host run completed functional assertions but its evidence reader failed on the CLI startup banner. The reader now ignores that exact banner, and the entire live test passed on a fresh fixture. Runtime behavior was not weakened to make the test pass.

Independent Fable review remains grouped with P3 as the owner requested. No development commit, merge or push.
