# P5 verification — 2026-09-11

Normal workflow execution, trusted successors and the assembled real-runtime proof passed. Source inventory and hashes remained unchanged during the final gates and proof. No development commit, merge or push was made.

## Repository checks

- Non-live unit gate: 1583 passed, 32 deselected.
- Process: 143 passed; real Beads: 25 passed; acceptance: 66 passed. Families overlap.
- Ruff and formatting passed over 217 files; strict mypy passed over 109 source files.
- Skill catalog, configuration parsing and scoped documentation checks passed.

Fable 5.1 high approved the grouped P4/P5 correction and final proof/CLI deltas, with no remaining important or critical findings. Its two important successor findings were reproduced and fixed: identical replay now uses saved pins before file/config resolution, and a different request key cannot reserve a second successor for the same predecessor after a crash. The final status renderer retains bounded integration identities and state instead of replacing oversized reports with a truncation marker.

## Actual live proof

The resumed test passed in 360.11 seconds. It retains earlier successful command evidence and revalidates current records; it does not present cached status as fresh. Eight distinct activation records prove real `claude-opus-5` and `gpt-6-astra` execution through wrapper receipts, process identities, Claude init data and native Codex session metadata.

The design/spec graph landed Markdown. Two independent child graphs overlapped in actual execution intervals; one emitted the distinct `doubt` boundary and applied an ordinary model decision before completing its writer. Both child results were collected. An explicitly prepared integration graph ran fresh implementation and review, received authenticated fixture approval, and landed `bc3a1a80514970d702d20dd6d8013ec05b520c32`. Final assertions preserve the design, both child contributions, child refs and the independently committed target change `3887e3edacd91ab23d1eb50dd07d21605d36c110`.

Signatures are synthetic fixture approvals, not production human approval. Signing material was not supplied in model inputs; the disclosed T1 filesystem boundary does not claim read containment.

Earlier failed attempts remain preserved: fixture setup cleanliness, empty stage descriptions, a model-name assertion against the wrong status schema, and oversized integration status. The apparent child stall was a snapshot taken before its following routing tick; the child reached its gate without an engine change. A deterministic characterization test preserves that behavior. Completed model work was retained through fixture continuation rather than repeated.

## Evidence and operation

The ignored execution workspace contains `p5-closeout-results.json` and matching logs, `p5-runtime-evidence.json`, `p5-runtime-identities.json`, `p5-final-target.json`, `p5-complete-source-digests.json`, and the Fable reports. Original and continuation locations are recorded in those artifacts. See [child operations](../../../usage/children.md), [phase bridge and integration](../../../usage/phase-bridge.md), and [P5 plan](../plans/P5.md).
