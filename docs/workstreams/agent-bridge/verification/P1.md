# P1 verification evidence

Verified on 2026-09-11 in the paused bridge worktree, with development changes uncommitted. Baseline HEAD is `71fea131387c73fb5ecdc9e3925114aa77954afd`. The main source checkout remained at `543e098118b6b6197ee39dfac96ddf5d846e826b`.

## Repository gates

| Command | Observed result |
|---|---|
| `uv run pytest -q -m "not bd and not live"` | 1432 passed, 27 deselected |
| `uv run pytest -q -m proc` | 123 passed |
| `uv run pytest -q -m bd` | 22 passed |
| `uv run pytest -q -m acceptance` | 66 passed |
| `uv run ruff check workflow_interpreter/ tests/` | Clean |
| `uv run ruff format --check workflow_interpreter/ tests/` | 191 files formatted |
| `MYPYPATH=. uv run mypy --strict --explicit-package-bases workflow_interpreter/` | 102 source files clean |

The test families overlap. Counts are not additive. Source digests were unchanged during these gates and the scoped review.

## Normal-entry real execution

The driver invoked the actual Foreman `phase-bridge` command against an independent disposable Git and Beads repository. It did not substitute an agent process. Both stages used Claude `claude-opus-5` to implement and Codex `gpt-6-astra` with low effort to review. Claude response metadata and saved Codex turn-context metadata confirmed those runtime/model identities. Each review accepted the actual change; the Supervisor observed successful pinned verification separately from model claims.

| Stage | Root | Starting commit | Landed commit | Result |
|---|---|---|---|---|
| `cr-5jo.1` | `cr-7g7` | `8079e03b69894fc4beb6bdef8f5252930e1bd678` | `6c15d9c1e5df0391e6d496f6d7d6c93c6a227e41` | Add implemented, reviewed, verified, landed and stage closed |
| `cr-5jo.2` | `cr-fd2` | `6c15d9c1e5df0391e6d496f6d7d6c93c6a227e41` | `a3685c78e7e35b62b0994ca4223a9e31c5e6aa7f` | Subtract added, add preserved, reviewed, verified, landed and stage closed |

The final target checkout was clean. These issue identifiers exist only in the proof store, not the development tracker. Ship approvals used a synthetic test signer restricted to that proof, not production human approval. This proves two available runtimes on this fixture; it does not establish every provider/profile as operational.

## Review

Fable `claude-fable-5-1` high reviewed the substantial P1 change. Two major findings were fixed: explicit retry of a persisted pending landing and check-policy setup guidance. The scoped correction review approved the batch with no critical or important defects. Parent verification and the live execution followed that review. Nonblocking observations remain visible in the review record; they do not authorize weakening historical evidence or checkout preservation.

## Evidence location

Full command logs, candidate source digests, initial and correction diffs, review texts, and live Git/Beads/agent artifacts are retained in the ignored `scratchpad/execution/agent-bridge-resume/` directory. Key records: `p1-fix-results.json`, `p1-fable-rereview.md`, `live-p1/results.json`, and `live-p1/runtime-evidence.json`. The live fixture driver and preparation script are preserved there for reconstruction. Private fixture signing material is excluded from documentation and repository history.
