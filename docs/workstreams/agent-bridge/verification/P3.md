# P3 verification — 2026-09-11

Independent child workflows passed the corrected full gates and grouped P2/P3 Fable 5.1 high review. Beads remains the durable owner/member store; there is no second database or child dependency scheduler.

- Non-live: 1498 passed, 29 deselected.
- Process: 136 passed; Beads: 23 passed; acceptance: 66 passed. Families overlap.
- Ruff and format clean (206 files); strict mypy clean (107 source files).
- Source digests unchanged during final verification and review.

Actual process tests cover overlapping isolated writers, concurrency caps, competing drivers, killed-driver recovery with unchanged identity/reservation, cancellation through launch publication and active decision processes, late evidence, and rejection of contradictory publications. A real bubblewrap-confined writer collects successfully. A real Beads workspace at the repository root stays clean with locks under Beads metadata.

The normal module entry exposes children start/status/collect/cancel/recover/drive. Collection verifies immutable output and repository artifact evidence; it does not authorize landing. See [child usage](../../../usage/children.md) for session-cap semantics, canonical workspace configuration, unknown-process pending cancellation, and offline migration of legacy lock files.

Earlier full runs exposed legacy composition/lock ordering and actual publication races. Their regressions were preserved and fixed; the file-backed test double now publishes crash-atomic snapshots rather than producing corruption unlike transactional Beads. Final evidence lives in the ignored execution workspace: `p3-races-final-results.json`, matching logs, `p23-race-fix-report.md`, `p23-fable-race-review.md`, and `p3-complete-source-digests.json`. Fable model usage identifies `claude-fable-5-1`.

P4 owns fresh integration and bridge binding/closure guards. P5 owns replacement lifecycle, templates and the assembled live proof. No development commits, merge or push.
