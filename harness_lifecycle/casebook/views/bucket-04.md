# Bucket 4 — Implementation & Refactoring

<!-- GENERATED by casebook.py build. Do not edit. Append a round instead. -->

**Current ruling: 9 considered → 5 adopted.**

## Adopted

### `code-simplification` — ADOPTED

*agent-skills · `7676817` · ruled in round-001-council-consolidation*

The only skill whose contract is change-how-it-reads-never-what-it-does, with an over-simplification guard.

### `incremental-implementation` — ADOPTED

*agent-skills · `7676817` · ruled in round-001-council-consolidation*

The only implementation discipline that fires WITHOUT a plan — 'any change touching more than one file'. The judge rejected the member that merged this into plan execution: that merge's spine opens 'validate plan', which presupposes a plan and destroys the no-plan trigger. Different actor too: subagent-driven-development is a controller protocol, this is the discipline of the agent writing the code. Contract-first slicing and the NOTICED-BUT-NOT-TOUCHING rule are unique to it.

### `security-and-hardening` — ADOPTED

*agent-skills · `7676817` · ruled in round-001-council-consolidation*

The corpus's single source of truth for treating input as untrusted. STRIDE, SSRF and TOCTOU coverage, OWASP LLM Top 10. Nothing else substitutes.

### `source-driven-development` — ADOPTED

*agent-skills · `7676817` · ruled in round-001-council-consolidation*

The only skill pinning framework facts to a version with citations and an UNVERIFIED flag. Distinct from research: this grounds decisions inline while building.

### `subagent-driven-development` — ADOPTED (merged)

*superpowers · `44c9b2d` · ruled in round-001-council-consolidation*

The only plan executor with bounded fix loops, capability escalation and an adjudication ledger that survives compaction. Superpowers' own writing-plans names it the recommended executor, and executing-plans defers to it explicitly.

**Absorbed:** `executing-plans`

**Changes made when adopting:**

- absorb executing-plans as a degraded inline mode for when subagents are unavailable

## Not adopted

### `executing-plans` — rejected

*superpowers · `44c9b2d` · ruled in round-001-council-consolidation*

Its own text says: 'If subagents are available, use subagent-driven-development instead of this skill.' Both council members cited that line independently. Survives as the degraded mode inside the winner.

### `frontend-ui-engineering` — rejected

*agent-skills · `7676817` · ruled in round-002-drop-ui-scope*

OUT OF SCOPE — the user is not a UI developer, so UI-specific capability has no use in this harness. This overturns round-001, which adopted it as 'the only UI skill' with a table of AI-aesthetic tells that has no counterpart in the corpus. That reasoning still holds on quality; it is scope, not merit, that removes it. Nothing else depends on it: it was a singleton family with no couplings into other adopted skills, so removal costs nothing elsewhere. Fully restorable from round-001 if UI work ever enters scope.

**Deliberately dropped:**

- the entire skill — accessible/responsive UI construction, WCAG compliance, component and layout patterns, state management, and the AI-aesthetic tells table

### `implement` — rejected

*mattpocock_skills · `84fdeff` · ruled in round-001-council-consolidation*

A 15-line wrapper. Every clause it contains (use TDD, typecheck often, review after, commit) is owned by a kept skill.

### `narrow-react-prop-types` — out of scope

*humanlayer_skills · `3c26291` · ruled in round-003-humanlayer-skills*

React/TypeScript prop-contract narrowing; UI work is out of scope for this harness (round-002). Its generic principle — live code paths are the source of truth, tests and stories adapt — already lives in our code-review and testing rules. Remains in inventory as humanlayer's reference actuator skill for the control-loop family.

**Deliberately dropped:**

- the entire skill

## Superseded rulings

- `frontend-ui-engineering` — round-002-drop-ui-scope replaced round-001-council-consolidation:e022: OUT OF SCOPE — the user is not a UI developer, so UI-specific capability has no use in this harness. This overturns round-001, which adopted it as 'the only UI 

- `browser-testing-with-devtools` — round-002-drop-ui-scope replaced round-001-council-consolidation:e028: OUT OF SCOPE — its declared trigger is 'building or debugging anything that runs in a browser', and its whole method is DOM inspection, console capture, network

- `performance-optimization` — round-002-drop-ui-scope replaced round-001-council-consolidation:e034: STAYS ADOPTED — scope-trimmed, not dropped. It covers frontend, backend, queries and databases; only the frontend portion is out of scope. Its load-bearing cont

- `show-me` — round-005-visual-explanation-merge replaced round-003-humanlayer-skills:e004: Adopted as .claude/skills/show-me, user-invoked only (disable-model-invocation: true). The round-003 defer precondition (record a prose-vs-diagram failure first

- `diagram-design` — round-005-visual-explanation-merge replaced round-004-visual-explanation:e001: Adopted in part into html-artifact — the geometry grammar and honesty doctrine, none of the machinery. references/svg-craft.md now carries the six connector rul

- `html-artifacts` — round-005-visual-explanation-merge replaced round-004-visual-explanation:e002: Adopted-merged into html-artifact per the line-level dossier: 14 of 39 components in reduced form, 4 REMOVEs executed on our side (spacing duplicate, framing re

- `diagram-design` — round-006-diagram-design-adoption replaced round-005-visual-explanation-merge:e002: User ruling: the full skill now ships as .claude/skills/diagram-design with disable-model-invocation: true — invoked only as /diagram-design, for standalone bra

- `diagram-design` — round-007-diagram-design-reversal replaced round-006-diagram-design-adoption:e001: User reversed round-006's full install before push: 2.9MB (mostly the 140-file assets tree) is too big for this harness. The standing ruling returns to round-00

