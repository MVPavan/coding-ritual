# Bucket 11 — Harness Routing & Agent-System Authoring

<!-- GENERATED by casebook.py build. Do not edit. Append a round instead. -->

**Current ruling: 5 considered → 2 adopted.**

## Adopted

### `using-superpowers` — ADOPTED (merged)

*superpowers · `44c9b2d` · ruled in round-001-council-consolidation*

The only content that makes the rest of the roster fire at all. Without a bootstrap, superpowers' own documentation says the skills are dead weight.

**Changes made when adopting:**

- regenerate the skill index from this roster — the old one lists skills that no longer exist
- keep the full invocation mandate INCLUDING the 1% threshold, the rationalization table and the precedence rule. One member wanted it weakened as over-triggering; the judge kept it because the only empirical evidence in the corpus points at UNDER-invocation as the observed failure. Over-firing costs a read; under-firing costs the discipline. Measure before weakening

**Deliberately dropped:**

- the per-harness platform reference files (codex, gemini, pi, antigravity tool refs) — harness-specific configuration, not skill content

### `writing-skills` — ADOPTED (merged)

*superpowers · `44c9b2d` · ruled in round-001-council-consolidation*

The only empirical method for proving a document actually changes behaviour. Chosen as base for that testing loop.

**Absorbed:** `writing-for-agents`

**Changes made when adopting:**

- absorb the information ladder, the two-loads model, pointer wording, the no-op test and negation scoping — the vocabulary for deciding what goes where

## Not adopted

### `ask-matt` — rejected

*mattpocock_skills · `84fdeff` · ruled in round-001-council-consolidation*

A router over a skill roster that no longer exists — stale by construction the moment the roster changed. Its one transferable asset, the phase-boundary tree, was rescued into context-engineering.

### `using-agent-skills` — rejected

*agent-skills · `7676817` · ruled in round-001-council-consolidation*

Same problem: an index over a changed roster. Its six core operating behaviours were checked one by one and all six are present in kept skills.

### `writing-for-agents` — rejected

*mattpocock_skills · `84fdeff` · ruled in round-001-council-consolidation*

Same job as writing-skills. Note this OVERRIDES the author, who files it under productivity rather than engineering — but mattpocock's own CLAUDE.md defines those folders as promotion tiers, not capability categories, so the foldering is not taxonomic evidence.

## Superseded rulings

- `frontend-ui-engineering` — round-002-drop-ui-scope replaced round-001-council-consolidation:e022: OUT OF SCOPE — the user is not a UI developer, so UI-specific capability has no use in this harness. This overturns round-001, which adopted it as 'the only UI 

- `browser-testing-with-devtools` — round-002-drop-ui-scope replaced round-001-council-consolidation:e028: OUT OF SCOPE — its declared trigger is 'building or debugging anything that runs in a browser', and its whole method is DOM inspection, console capture, network

- `performance-optimization` — round-002-drop-ui-scope replaced round-001-council-consolidation:e034: STAYS ADOPTED — scope-trimmed, not dropped. It covers frontend, backend, queries and databases; only the frontend portion is out of scope. Its load-bearing cont

