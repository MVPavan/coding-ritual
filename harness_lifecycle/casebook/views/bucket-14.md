# Bucket 14 — Human Learning, Content & Conversation

<!-- GENERATED by casebook.py build. Do not edit. Append a round instead. -->

**Current ruling: 4 considered → 2 adopted.**

## Adopted

### `diagram-design` — ADOPTED

*diagram-design · `4b57361` · ruled in round-006-diagram-design-adoption*

User ruling: the full skill now ships as .claude/skills/diagram-design with disable-model-invocation: true — invoked only as /diagram-design, for standalone branded diagram deliverables (38 visual types, drawio/Mermaid import, brand onboarding, export). This does not reopen round-005's merge: figures inside html-artifact documents still follow references/svg-craft.md and the html-artifact invariants; diagram-design's own design system (Google Fonts, atomic-tangerine skin, min-width layouts) applies only inside its explicitly invoked outputs, which is why model invocation is disabled. The round-005 concern that its conventions conflict with our invariants is answered by the slash-only boundary, not withdrawn.

**Absorbed:** `skills/diagram-design/** (whole directory)`, `repo LICENSE`, `repo THIRD_PARTY_LICENSES.md`

**Changes made when adopting:**

- frontmatter: disable-model-invocation: true
- SKILL.md par.2 don't-use row: wiretext pointer -> /show-me (wiretext not installed here)
- SKILL.md 3x: <repo-root>/scripts/verify-*.py -> reference_harnesses/diagram-design/scripts/ (the submodule is our checkout)
- SKILL.md par.12: output saved under scratchpad/ unless the user or a calling skill names a destination
- skill-catalog.py: 6 allowlist entries so upstream references stay byte-identical (3 unadopted command pointers, /css2 font URL, /regenerate-examples, /skills path fragment)

**Deliberately dropped:**

- commands/ (export-diagram, import-drawio, import-mermaid, profile) — deferred in ledger; the installed skill routes to the same references
- plugin wrapper and marketplace manifests — we distribute via mvp-plugin
- repo-level verify/lint/test scripts and vendored icon sources — remain available read-only in the reference submodule

### `show-me` — ADOPTED (merged)

*humanlayer_skills · `3c26291` · ruled in round-005-visual-explanation-merge*

Adopted as .claude/skills/show-me, user-invoked only (disable-model-invocation: true). The round-003 defer precondition (record a prose-vs-diagram failure first) is deliberately bent, with the mitigation that a slash-only skill cannot misfire — the downside of early adoption is one catalog line, not variance. Round-004 strengthened the case: diagram-design hands its own low end to a nonexistent wiretext skill, confirming the inline niche is real and unowned; the Mermaid conflict dissolved (chat surface vs rendered figure); and the diff-shaped-to-topic device has no substitute anywhere in the corpus.

**Absorbed:** `show-me (humanlayer)`

**Changes made when adopting:**

- description rewritten to triggers only
- Mermaid marked client-dependent, never terminal
- handoff bound to html-artifact's all-hold gate (critic finding 3)
- five inline forms merged, file-path/hook annotations kept

**Deliberately dropped:**

- one-sentence HTML escalation (html-artifact owns that end)
- macOS-only open command
- model auto-invocation (revisit with observed usage)

## Not adopted

### `teach` — out of scope

*mattpocock_skills · `84fdeff` · ruled in round-001-council-consolidation*

Not a software skill. It builds a multi-session personal learning workspace with missions, HTML lessons, learning records and spaced retrieval; its own examples are yoga and theoretical physics. Out of scope by KIND, not by redundancy — nothing duplicates it. Retained intact and self-contained if a personal-productivity catalog is ever wanted.

### `wait-what` — out of scope

*mattpocock_skills · `84fdeff` · ruled in round-001-council-consolidation*

A 7-line conversational-repair macro: re-pitch the last message in Simplified Technical English. Output is consumed by a person, not the software system. Nearly free to reinstate later — user-invoked, zero always-on context cost.

## Superseded rulings

- `frontend-ui-engineering` — round-002-drop-ui-scope replaced round-001-council-consolidation:e022: OUT OF SCOPE — the user is not a UI developer, so UI-specific capability has no use in this harness. This overturns round-001, which adopted it as 'the only UI 

- `browser-testing-with-devtools` — round-002-drop-ui-scope replaced round-001-council-consolidation:e028: OUT OF SCOPE — its declared trigger is 'building or debugging anything that runs in a browser', and its whole method is DOM inspection, console capture, network

- `performance-optimization` — round-002-drop-ui-scope replaced round-001-council-consolidation:e034: STAYS ADOPTED — scope-trimmed, not dropped. It covers frontend, backend, queries and databases; only the frontend portion is out of scope. Its load-bearing cont

- `show-me` — round-005-visual-explanation-merge replaced round-003-humanlayer-skills:e004: Adopted as .claude/skills/show-me, user-invoked only (disable-model-invocation: true). The round-003 defer precondition (record a prose-vs-diagram failure first

- `diagram-design` — round-005-visual-explanation-merge replaced round-004-visual-explanation:e001: Adopted in part into html-artifact — the geometry grammar and honesty doctrine, none of the machinery. references/svg-craft.md now carries the six connector rul

- `html-artifacts` — round-005-visual-explanation-merge replaced round-004-visual-explanation:e002: Adopted-merged into html-artifact per the line-level dossier: 14 of 39 components in reduced form, 4 REMOVEs executed on our side (spacing duplicate, framing re

- `diagram-design` — round-006-diagram-design-adoption replaced round-005-visual-explanation-merge:e002: User ruling: the full skill now ships as .claude/skills/diagram-design with disable-model-invocation: true — invoked only as /diagram-design, for standalone bra

