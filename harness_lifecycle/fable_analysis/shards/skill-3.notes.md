# Shard notes — skill-3 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumptions: scores judge reusable-harness value, not whether the topic is generally useful. Vendor/version-sensitive instructions were penalized when they lacked verification or conflicted with live local assets.

Strongest retained cluster: the existing graph-first, grill-me, html-artifact, planning, and phase-execution skills. The two Git publishing rows should become one compact capability. Product-capability, onboarding, iterative-retrieval, architecture assessment, and handoff contribute patterns to existing local workflows rather than warranting new standalone skills.

Weakest rows: opensource-pipeline, hook-development, MCP integration, plugin-settings, the three large Python/PyTorch catalogs, knowledge-ops, and rclone. Their main failure modes are false safety, stale schemas, arbitrary defaults, duplicated policy, or authority expansion.

Review was read-only. All 27 rows were preserved in input order. Several Compound Engineering paths were stale, so current renamed files or the last version before upstream deletion were inspected. The working tree was already dirty and was left untouched.
