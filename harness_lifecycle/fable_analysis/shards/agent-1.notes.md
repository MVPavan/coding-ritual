# Shard notes — agent-1 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumptions: scores judge reusable capability content rather than harness syntax. `adopt` means retain as a distinct capability; `merge` means preserve only its incremental techniques in an existing local capability; `rewrite` means the problem deserves a capability but the source prompt is unsafe or stack-misaligned.

Evidence: all supplied files were read. Stale Compound Engineering paths were resolved to current skill-local personas; removed bug-reproduction, simplicity, and migration agents were recovered from git history. Local overlap was checked across `.codex/`, `.claude/`, the canonical harness guide, and relevant planning, debugging, review, research, and verification skills.

Weakest rows are the broad `architect`, proactive JS-heavy `code-simplifier`, TypeScript-specific `doc-updater`, and generic documentation writer. The main consolidation clusters are: code-review core, document review, docs research, database-change safety, code simplicity, and maximum-effort generalists. No files or tracker state were intentionally modified.
