# Shard notes — command-5 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumptions: scores apply to the row's command surface, not automatically to the richer skill or script it delegates to. For stale paths, I recovered the last available command from pinned Git history and inspected the delegated skill when needed.

Evidence: read all current source documents; adjacent Ralph-loop, code-intel, and skill-health implementations; local overlap candidates in `.codex/`, `.claude/`, `mvp-harness/`, and `harness_lifecycle/`; and the repo's canonical minimal-surface guidance. No write tools or mutating commands were used. Final `git status` showed existing unrelated harness-analysis changes.

Weakest rows: the README non-capability, deprecated `rules-distill`/`tdd`/`verify`/`write-plan` shims, unsafe `sync-tutorials`, and the six repo-specific report-maintenance workflows. Cluster relationships: `quiz-me` and `teach-me` should feed one teaching-and-retention synthesis; the five Claude report drift commands form one rejectable vendor-doc cluster; `development-workflows` is superseded by the local harness lifecycle; code-intel setup is the sole adopt-as-is row.
