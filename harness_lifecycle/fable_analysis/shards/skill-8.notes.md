# Shard notes — skill-8 (coordinator-authored stub)

The skill-8 worker (Fable-5 xhigh, wave 1, 2026-07-06) wrote all 27 row
evaluations incrementally but was killed by the Claude session limit before it
could write its shard notes. The evaluations in `skill-8.row_evaluations.jsonl`
are complete and passed schema + coverage verification; the worker's own
assumptions/evidence commentary was lost with the process and is NOT
reconstructed here — per-row `evidence_notes` in the JSONL are the surviving
evidence record.
