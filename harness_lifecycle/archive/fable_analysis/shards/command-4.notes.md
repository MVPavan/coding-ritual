# Shard notes — command-4 (evaluator: Codex GPT-5.6-Sol xhigh)

All 25 source definitions were read. The two stale paths (`orchestrate`, `prompt-optimize`) were resolved under `legacy-command-shims/`. Strongest reusable material is in business-rule extraction, modernization preflight, behavior-preserving transformation, runtime uplift, and loop observability; these still require merging, rewriting, or deferral because of suite coupling. Weakest rows are the legacy shims, loop-start stub, static model router, instinct-management commands, unsafe smart commit, and heavyweight multi-model mega-workflows. The four `multi-*` commands form one overlapping workflow cluster; `learn`, `projects`, `promote`, and `prune` form one competing knowledge-lifecycle cluster. No write commands were run; `git status --short` remains dirty with unrelated existing analysis, Beads, harness, and submodule changes.
