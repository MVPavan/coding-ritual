# Shard notes — agent-3 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumptions: each multi-source row was scored as one logical capability; the current pinned source took precedence, with Git history used only where the named capability had been removed. Evidence: read every named capability body or its relocated/historical equivalent, the project overlay/canon, and the closest local agents, skills, rules, hooks, and templates. Strongest merge candidates are spec-flow analysis, characterization testing, focused test review, CLI agent-readiness, and data-migration safety. Weakest rows are the auto-memory observer, hard-coded constitutional validator, design iterator, workflow-research agent, Figma sync, E2E runner, and GAN pair. Cluster relationships: tdd-guide/test-engineer, observer/conversation-analyzer, analyzer/comparator, both CLI reviewers, both Figma reviewers, and the GAN evaluator/generator should be considered together. No writes were performed; the working tree was already non-clean and was left untouched.
