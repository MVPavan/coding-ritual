# Shard notes — rule-2 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumptions: scores measure value to this Python-centric reusable harness, not intrinsic usefulness inside each source repository. For the local curation row, `adopt` means retain. All stale `rules/zh/...` paths were resolved to the current `docs/zh-CN/rules/...` files and checked against their English counterparts.

Weak rows: `presentation` is hard-coded foreign-deck routing; `README` is misclassified installer documentation; Remotion measurement and React state patterns are outside scope. The localized rows are documentation duplicates, several already stale relative to English.

Cluster relationships: retain `harness-curation`; merge only small semantic-style and browser-verification additions into `html-artifact`; rewrite web security as a conditional Python-web baseline. The existing local HTML design guidance is superior to `web/design-quality` for document artifacts.

The sandbox remained read-only. `bd ready` could not open its embedded Dolt database because initialization encountered the read-only filesystem, so no issue lifecycle operation was attempted.
