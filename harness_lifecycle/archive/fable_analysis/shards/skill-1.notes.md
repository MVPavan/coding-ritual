# Shard notes — skill-1 (evaluator: Codex GPT-5.6-Sol xhigh)

Reviewed all 27 rows against live repository sources and local counterparts. Stale Compound Engineering paths were recovered from submodule git history; current replacements were inspected where available (`ce-code-review`, folded session-history support).

Strongest keep/adopt candidates: `ak-guide`, `beads`, local `brainstorming`, `check-invariants`, and `api-connector-builder`. Weakest rows: `ai-regression-testing`, `benchmark`, `canary-watch`, `autonomous-loops`, `changelog`, and `claude-automation-recommender` because they are language-misaligned, declarative without implementation, unsafe, or already superseded.

Cluster relationships: local brainstorming and planning are substantially leaner than the CE variants; CE learning capture contains a useful idea but should merge into the existing verified-learnings surface; CE review contributes confidence/dedup patterns but not its mutation-heavy orchestration. No mutating commands were executed. The working tree was already dirty when checked and was left untouched.
