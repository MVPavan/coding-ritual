# Shard notes — agent-4 (evaluator: Codex GPT-5.6-Sol xhigh)

Assumption: scores measure value to this repository's reusable, Python-centric harness—not quality inside a candidate's original niche workflow.

Evidence inspected: all 27 candidate files were read. Three stale Compound Engineering paths were resolved to their current skill-reference locations. Local comparisons included brainstorming, planning, document review, implementer/spec/code-review agents, docs-researcher, harness-evaluate, check-invariants, Python safety, mvp-plugin doctor, and the canonical harness-design guidance.

Weakest rows: `gan-planner`, `healthcare-reviewer`, `technical-cto-advisor`, and the JS/Rails-centric `security-sentinel`. Their shallow positive signals do not survive scope, safety, or evidence review.

Cluster relationships: merge the five `claude-code-docs-drift` agents into one parameterized audit; consolidate the three `open-source-release-prep` roles into one gated workflow while preserving independent verification; fold the useful product/UX checklists into existing brainstorming or document review rather than adding personas.

The strongest new pattern is `version-delta-analyst`'s official-changes ∩ actual-code-usage method, rewritten around Python. The existing bounded `implementer` remains the strongest ready-to-keep item.

Read-only constraint was maintained. Final `git status --short` showed a pre-existing dirty worktree; no files were written or modified by this evaluation.
